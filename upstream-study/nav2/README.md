# Nav2 Study

Focus: Behavior Tree orchestration, planner/controller server boundaries, recovery behaviors, costmaps, action interfaces, and where AI-level task planning should connect without weakening deterministic navigation/safety.

Status: scaffold only.
