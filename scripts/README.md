# Scripts

Reusable automation for setup, data conversion, training, evaluation, deployment, and reporting.

Planned structure:

```text
scripts/
├── setup/
├── data/
├── train/
├── eval/
├── deploy/
└── README.md
```

Scripts should be idempotent when practical, configurable from CLI/YAML, and reusable across projects.
